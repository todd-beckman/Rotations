//  HEADERS:
bool rand(odds); // [0, odds)
int multiHit(mon);
void statusChance(mon, odds, status);


Move : {
    //  Returns whether to permit this move
    onMoveBegin : function (Mon user, Mon target),

    //  Decide if waiting out this turn
    onCharge : function(user),

    //  Decide if hitting a semi-invulnerable mon
    onTestSemiInvulnerable : function(user, target),


    //  Returns whether to allow protect
    onProtect : function (Mon user, Mon target),

    //  Returns a temporary stat change
    onAtkMod : function(user, atk, target),
    onDefMod : function(user, def, target),
    onSpAtkMod : function(user, spatk, target),
    onSpDefMod : function(user, spdef, target),
    onSpdMod : function(user, spd, target),
    onCritMod : function(user, crit, target),
    onEvaMod : function(user, eva, target),
    onAccMod : function(user, acc, target),

    //  Allow power modification
    onPowMod : function(user, target),

    //  Move is successful
    onSuccess : function (user, target, damage)
    onMoveFail : function (user, target),


    
}


Field : {
    weather : {
        type : String,
        duration : int
    }
    gravity : int
}
