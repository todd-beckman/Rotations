function Decision(option, choice, direction, mega) {
    //  Switch or Fight
    this.option = option;
    //  Move slot or Switch slot
    this.choice = choice;
    //  Rotate direction (optional)
    this.direction = direction;
    //  Mega Evolution (optional)
    this.mega = mega;
}
